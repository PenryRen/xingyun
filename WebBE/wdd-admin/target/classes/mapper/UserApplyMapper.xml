<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.UserApplyMapper">

  <select id="getUserApply" resultType="com.mindskip.wdd.domain.UserApply">
    select  *  from t_user_apply
    where deleted =0 and user_id = #{userId} and apply_id = #{applyId}
      limit 1
  </select>

  <select id="userIdListByApplyId" parameterType="java.lang.Integer" resultType="java.lang.Integer">
    select user_id
    from t_user_apply
    where deleted = 0 and apply_id = #{id}
  </select>

</mapper>