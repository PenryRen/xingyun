<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.ForumArchiveMapper">


  <select id="selectRootTree" resultType="com.mindskip.wdd.domain.ForumArchive" >
    SELECT  *  FROM t_forum_archive
    where deleted= 0 and parent_id is null
    order by item_order asc
  </select>



  <select id="getByParentId" resultType="com.mindskip.wdd.domain.ForumArchive" >
    SELECT  *  FROM t_forum_archive
    where deleted= 0 and parent_id = #{id}
    order by item_order asc
  </select>


  <select id="getForumArchiveList" resultType="com.mindskip.wdd.domain.ForumArchive" >
    SELECT  *  FROM t_forum_archive
    where deleted= 0
    and id in
    <foreach collection="idList" separator="," open="(" close=")" item="id">
      #{id,jdbcType=VARCHAR}
    </foreach>
  </select>

</mapper>