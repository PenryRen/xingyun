<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.DepartmentMapper">

  <select id="getRootDepartment" resultType="com.mindskip.wdd.domain.Department" >
    SELECT  *  FROM t_department
    where deleted= 0 and parent_id is null
    order by item_order asc
  </select>


  <select id="getDepartmentByParentId" resultType="com.mindskip.wdd.domain.Department" >
    SELECT  *  FROM t_department
    where deleted= 0 and parent_id = #{id}
    order by item_order asc
  </select>


  <update id="updateLevel">
    update t_department set level = regexp_replace(level,#{regexLevel},#{targetLevel})
  </update>

  <select id="getByLevel" resultType="com.mindskip.wdd.domain.Department">
    SELECT  *  FROM t_department
    where deleted= 0 and level = #{level}
    limit 1
  </select>

  <update id="deleteByLevel">
    update t_department set deleted = 1 where level like concat(#{level},'%')
  </update>


  <select id="getDepartmentById" resultType="com.mindskip.wdd.domain.Department" >
    SELECT  *  FROM t_department
    where deleted= 0
    and id in
    <foreach item="item" collection="idList" open="(" separator="," close=")">
      #{item}
    </foreach>
    order by item_order asc
  </select>

</mapper>