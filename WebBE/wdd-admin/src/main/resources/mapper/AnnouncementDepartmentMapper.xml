<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.AnnouncementDepartmentMapper">

  <select id="getDepartmentByAnnouncementId" resultType="com.mindskip.wdd.domain.other.KeyValue" parameterType="java.lang.Integer">
    SELECT d.level as name ,d.name as nameSecond  , d.id as value from t_department d,t_announcement_department ad
    WHERE d.id=ad.department_id and ad.deleted =0 and d.deleted=0 and ad.announcement_id=#{announcementId}
  </select>


  <update id="updateDeleteByAnnouncementId" parameterType="java.lang.Integer">
    update t_announcement_department
    set  deleted = 1
    where announcement_id = #{announcementId}
  </update>

</mapper>