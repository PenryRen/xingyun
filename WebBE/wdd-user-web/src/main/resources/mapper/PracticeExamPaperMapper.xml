<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.PracticeExamPaperMapper">

    <select id="waiteAnswer" resultType="com.mindskip.wdd.domain.PracticeExamPaper">
        SELECT pep.*  FROM t_practice_exam_paper pep
        <where>
            pep.deleted= 0 and pep.practice_build_id = #{practiceBuildId}
            and not exists (select 1 from t_practice_exam_paper_answer pepa
                            where pep.id = pepa.practice_exam_paper_id and pepa.deleted= 0
                                  and pepa.create_user = #{userId} and pepa.practice_build_id = #{practiceBuildId} )
        </where>
    </select>

</mapper>
