<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.ApplyArchiveMapper">


  <select id="getRootApplyArchive" resultType="com.mindskip.wdd.domain.ApplyArchive" >
    SELECT  *  FROM t_apply_archive
    where deleted= 0 and parent_id is null
    order by item_order asc
  </select>



  <select id="getApplyArchiveByParentId" resultType="com.mindskip.wdd.domain.ApplyArchive" >
    SELECT  *  FROM t_apply_archive
    where deleted= 0 and parent_id = #{id}
    order by item_order asc
  </select>

  <update id="updateLevel">
    update t_apply_archive set level = regexp_replace(level,#{regexLevel},#{targetLevel})
  </update>

  <select id="getByLevel" resultType="com.mindskip.wdd.domain.ApplyArchive">
    select  *  from t_apply_archive
    where deleted= 0 and level = #{level}
    limit 1
  </select>

  <update id="deleteByLevel">
    update t_apply_archive set deleted = 1 where level like concat(#{level},'%')
  </update>


</mapper>