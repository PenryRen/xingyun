<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.TrainItemUserMapper">

  <select id="getTrainItemUserList" parameterType="java.lang.Long" resultType="com.mindskip.wdd.domain.TrainItemUser">
    select  *  from t_train_item_user
    where deleted = 0 and train_user_id = #{trainUserId}
  </select>


  <insert id="insertList">
    insert into t_train_item_user (train_user_id, train_id,train_item_id, target_id, target_type,current_number
    , pass_number, max_number,item_order, status, create_user,create_time, deleted, create_department_id,user_target_id,complete_time,allow_count)
    values
    <foreach collection="trainItemUserList" item="item"   separator=",">
      (#{item.trainUserId}, #{item.trainId},#{item.trainItemId}, #{item.targetId}, #{item.targetType}
      ,#{item.currentNumber}, #{item.passNumber}, #{item.maxNumber},#{item.itemOrder}, #{item.status}
      , #{item.createUser},#{item.createTime}, #{item.deleted}, #{item.createDepartmentId}, #{item.userTargetId}, #{item.completeTime}, #{item.allowCount})
    </foreach>
  </insert>


  <update id="updateList">
    <foreach collection="trainItemUserList" item="item"  separator=";" close=";">
      update t_train_item_user set item_order = #{item.itemOrder} , deleted = #{item.deleted}
      where id = #{item.id}
    </foreach>
  </update>


  <select id="getTrainItemUser"  resultType="com.mindskip.wdd.domain.TrainItemUser">
    select  *  from t_train_item_user
    where deleted = 0  and id = #{trainUserId} and create_user = #{userId}
    <if test="status != null">
      and status = #{status,jdbcType=INTEGER}
    </if>
    limit 1
  </select>


  <select id="trainCourseWareGoingCount"  resultType="java.lang.Integer">
    select count(*)
    from t_train_item_user
    where deleted = 0 and status = 1 and target_type = 1 and train_id = #{trainId} and create_user = #{userId}
    limit 1
  </select>



    <update id="clearTrainItemUser">
    update t_train_item_user set deleted = 1
    where  train_user_id = #{trainUserId}
  </update>

</mapper>