<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.ExamPaperMapper">

  <update id="deleteByExamPaperBuildId" parameterType="java.lang.Long">
    update t_exam_paper
    set  deleted = 1
    where exam_paper_build_id = #{examPaperBuildId,jdbcType=BIGINT}
  </update>


  <select id="paperAllUserCount" resultType="java.lang.Integer" >
    SELECT count(*) from t_user where   deleted= 0  and system_role = 1 and status = 1 and (
      id in (select user_id from t_exam_paper_user  where deleted = 0 and exam_paper_id = #{examPaperId})
      or
      department_id in (select department_id from t_exam_paper_department  WHERE  deleted=0 and exam_paper_id = #{examPaperId} )
    )
  </select>


</mapper>