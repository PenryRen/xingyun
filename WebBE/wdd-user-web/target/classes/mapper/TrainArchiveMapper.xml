<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.TrainArchiveMapper">


  <select id="selectRootTree" resultType="com.mindskip.wdd.domain.TrainArchive" >
    SELECT  *  FROM t_train_archive
    where deleted= 0 and parent_id is null
    order by item_order asc
  </select>


  <select id="getByParentId" resultType="com.mindskip.wdd.domain.TrainArchive" >
    SELECT  *  FROM t_train_archive
    where deleted= 0 and parent_id = #{id}
    order by item_order asc
  </select>


  <select id="getTrainArchiveByLevel" resultType="com.mindskip.wdd.domain.TrainArchive">
    SELECT  *  FROM t_train_archive
    where deleted= 0 and level like concat(#{level},'%')
  </select>

</mapper>