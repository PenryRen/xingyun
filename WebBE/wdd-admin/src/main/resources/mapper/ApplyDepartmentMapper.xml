<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.ApplyDepartmentMapper">

  <select id="getDepartmentByApplyId" resultType="com.mindskip.wdd.domain.other.KeyValue" parameterType="java.lang.Integer">
    SELECT d.name as name ,d.name as nameSecond  , d.id as value from t_department d,t_apply_department ad
    WHERE d.id=ad.department_id and ad.deleted =0 and d.deleted=0 and ad.apply_id=#{applyId}
  </select>


  <update id="updateDeleteByApplyId" parameterType="java.lang.Integer">
    update t_apply_department
    set  deleted = 1
    where apply_id = #{applyId}
  </update>



  <select id="getApplyDepartmentByApplyId" resultType="com.mindskip.wdd.domain.ApplyDepartment" parameterType="java.lang.Integer">
    select  *  from t_apply_department
    where deleted=0 and apply_id = #{applyId}
  </select>

</mapper>