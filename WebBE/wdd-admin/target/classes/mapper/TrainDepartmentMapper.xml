<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindskip.wdd.repository.TrainDepartmentMapper">

  <select id="getTrainDepartmentList" parameterType="java.lang.Integer" resultType="com.mindskip.wdd.domain.TrainDepartment">
    select  *  from t_train_department
    where deleted =0 and train_id = #{trainId}
  </select>

  <update id="clearTrainDepartment">
    update t_train_department
    set deleted = 1
    where train_id = #{trainId}
  </update>

</mapper>